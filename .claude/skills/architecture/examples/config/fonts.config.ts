// Centralized font setup.
// Replace the placeholder below with your framework's font loader.

// Example for Next.js:
//
//   import { Inter, JetBrains_Mono } from "next/font/google";
//
//   export const sans = Inter({ subsets: ["latin"], variable: "--font-sans" });
//   export const mono = JetBrains_Mono({ subsets: ["latin"], variable: "--font-mono" });

export const sans = {
  variable: "--font-sans",
  className: "font-sans",
};

export const mono = {
  variable: "--font-mono",
  className: "font-mono",
};
